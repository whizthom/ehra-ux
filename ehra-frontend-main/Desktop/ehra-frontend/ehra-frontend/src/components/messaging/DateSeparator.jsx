export default function DateSeparator({ label }) {
  return (
    <div
      style={{
        textAlign: "center",
        margin: "18px 0",
        color: "var(--text-secondary,#6f8b81)",
        fontSize: 12,
      }}
    >
      {label}
    </div>
  );
}
