import { useEffect, useMemo, useState } from "react";
import { useNavigate } from "react-router-dom";
import { Dashboard, Products, Inventory, Orders, Customers, POS, Expenses, Suppliers, Reports, Store, Settings, ProductModal, ExpenseModal, PurchaseModal, CustomerModal, SupplierModal } from "./retail/RetailSections";
import { getProducts, createProduct, updateProduct, deleteProduct, getOrders, getCustomers, getStorefront, createCustomer, updateCustomer, deleteCustomer } from "../api/commerceApi";
import { getExpenses, addExpense, updateExpense, deleteExpense, getSuppliers, deleteSupplier, updateSupplier, addSupplier, getMovements, adjustStock, getSales, createSale, getPurchases, createPurchase, refundSale, reversePurchase, getRetailContext } from "../api/retailApi";
import { getBusinessType, getMyBusinessProfile } from "../api/businessApi";
import s from "./RetailWorkspace.module.css";

const today = () => new Date().toISOString().slice(0, 10);
const NAV = [["Dashboard","⌂",null],["Store","◈","settings"],["Products","▦","inventory"],["Inventory","□","inventory"],["Orders","↗","orders"],["Customers","♙","customers"],["Sales / POS","₦","sales"],["Expenses","≡","finance"],["Suppliers","⇄","finance"],["Reports","▥","finance"],["Settings","⚙","settings"]];
const PERM = { settings:"canSettings", inventory:"canInventory", orders:"canOrders", customers:"canCustomers", sales:"canSales", finance:"canFinance" };

export default function RetailWorkspace() {
  const nav = useNavigate();
  const [tab,setTab] = useState("Dashboard");
  const [business,setBusiness] = useState(null), [type,setType] = useState(null), [context,setContext] = useState(null), [businessCurrency,setBusinessCurrency] = useState("NGN");
  const [data,setData] = useState({products:[],orders:[],customers:[],expenses:[],suppliers:[],movements:[],sales:[],purchases:[]});
  const [report,setReport] = useState(null), [store,setStore] = useState(null), [loading,setLoading] = useState(true), [modal,setModal] = useState(null), [editing,setEditing] = useState(null), [query,setQuery] = useState("");
  const money = n => `${businessCurrency} ${Number(n || 0).toLocaleString(undefined, { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`;
  const allowed = useMemo(() => { const a=new Set(["Dashboard"]); if(!context)return a; NAV.forEach(([name,,p])=>{if(!p||context.owner||context[PERM[p]])a.add(name)}); return a; },[context]);
  const goBack = () => nav(context?.owner ? "/dashboard" : "/my-dashboard");
  const load = async () => {
    const jobs = [["products",getProducts(true)], ["orders",getOrders()], ["customers",getCustomers()]];
    if(context?.owner || context.canFinance) jobs.push(["expenses",getExpenses()], ["suppliers",getSuppliers()], ["purchases",getPurchases()]);
    if(context?.owner || context.canInventory) jobs.push(["movements",getMovements()]);
    if(context?.owner || context.canSales) jobs.push(["sales",getSales()]);
    if(context?.owner || context.canSettings) jobs.push(["store",getStorefront()]);
    const results=await Promise.allSettled(jobs.map(([,p])=>p)); const next={...data};
    results.forEach((r,i)=>{const [k]=jobs[i];if(r.status==="fulfilled"){if(k==="store"){setStore(r.value.data);if(r.value.data?.currency)setBusinessCurrency(r.value.data.currency);}else next[k]=r.value.data||[];}}); setData(next);
  };
  useEffect(()=>{let dead=false;(async()=>{try{const {data:c}=await getRetailContext();if(dead)return;setContext(c);setType({businessType:c.businessType,businessName:c.businessName});setBusiness({id:c.businessId,name:c.businessName});if(c.owner){const [t,b]=await Promise.all([getBusinessType(),getMyBusinessProfile()]);if(!dead){setType(t.data);setBusiness(b.data);setBusinessCurrency(b.data?.currency||c.businessCurrency||"NGN");}}}catch(e){if(!dead)setContext({denied:true,message:e?.response?.data?.message||"You do not have access to this Retail Workspace."});}finally{if(!dead)setLoading(false);}})();return()=>{dead=true};},[]);
  useEffect(()=>{if(context?.owner||context?.canWorkspace)load();/* permission-aware load */ // eslint-disable-next-line react-hooks/exhaustive-deps
  },[context]);
  useEffect(()=>{if(!allowed.has(tab))setTab("Dashboard");},[allowed,tab]);
  const active=data.products.filter(p=>p.status==="ACTIVE"); const low=active.filter(p=>p.trackInventory&&Number(p.stockQuantity)<=Number(p.lowStockThreshold||5)); const pending=data.orders.filter(o=>["PENDING","CONFIRMED","PROCESSING"].includes(o.status));
  const todaySales=data.sales.filter(x=>String(x.createdAt||"").slice(0,10)===today()).reduce((n,x)=>n+Number(x.total||0),0); const monthSales=data.sales.filter(x=>String(x.createdAt||"").slice(0,7)===today().slice(0,7)).reduce((n,x)=>n+Number(x.total||0),0);
  const filtered=(arr,fields)=>arr.filter(x=>fields.some(k=>String(x[k]||"").toLowerCase().includes(query.toLowerCase()))); const runLoad=()=>load();
  if(loading)return <div className={s.loading}><div className={s.spinner}/><span>Preparing your retail workspace</span></div>;
  if(context?.denied||(!context?.owner&&!context?.canWorkspace))return <div className={s.loading}><h2>Retail Workspace access is not enabled for you</h2><p>{context?.message||"Your employer has not granted Retail Workspace access."}</p><button className={s.primary} onClick={goBack}>Back to Ehral</button></div>;
  if(type?.businessType!=="RETAIL")return <div className={s.loading}><h2>Retail Workspace is not enabled</h2><button className={s.primary} onClick={()=>nav("/business-setup")}>Set up your business</button></div>;
  return <div className={s.app}><aside className={s.sidebar}><div className={s.logo}><b>e</b><div><strong>Ehral</strong><small>Retail</small></div></div><div className={s.businessSwitch}><span>BUSINESS</span><strong>{business?.name||"Your business"}</strong><em>Retail <i>Locked</i></em></div><nav>{NAV.filter(([n])=>allowed.has(n)).map(([n,i])=><button key={n} className={tab===n?s.navActive:""} onClick={()=>setTab(n)}><b>{i}</b>{n}</button>)}</nav><button className={s.back} onClick={goBack}>← <span>Back to Ehral</span></button></aside><main className={s.main}><header className={s.header}><div><span className={s.kicker}>RETAIL BUSINESS WORKSPACE</span><h1>{tab}</h1><p>{business?.name} <span>•</span> {context.owner?"Business owner access.":`${context.role||"Employee"} access granted by your employer.`}</p></div><div className={s.headerActions}><button className={s.outline} onClick={goBack}>Ehral Dashboard</button><div className={s.avatar}>{(business?.name||"B").slice(0,1).toUpperCase()}</div></div></header><div className={s.content}>{tab==="Dashboard"&&<Dashboard todaySales={todaySales} monthSales={monthSales} active={active} low={low} pending={pending} data={data} setTab={setTab} money={money} allowedTabs={allowed}/>} {tab==="Store"&&<Store store={store} setStore={setStore} business={business} owner={context.owner} onCurrencyChange={c=>{setBusinessCurrency(c);setBusiness(prev=>prev?{...prev,currency:c}:prev)}} money={money}/>} {tab==="Products"&&<Products items={filtered(active,["name","sku","category","brand"])} query={query} setQuery={setQuery} onAdd={()=>{setEditing(null);setModal("product")}} onEdit={p=>{setEditing(p);setModal("product")}} onDelete={async p=>{if(confirm(`Archive ${p.name}?`)){await deleteProduct(p.id);runLoad()}}} money={money}/>} {tab==="Inventory"&&<Inventory data={data} products={active} onAdjust={async(p,t,q,n)=>{await adjustStock(p.id,t,q,n);runLoad()}} money={money}/>} {tab==="Orders"&&<Orders items={data.orders} query={query} setQuery={setQuery} money={money} canFinance={context.owner || context.canFinance}/>} {tab==="Customers"&&<Customers items={data.customers} onAdd={()=>{setEditing(null);setModal("customer")}} onEdit={x=>{setEditing(x);setModal("customer")}} onDelete={async id=>{await deleteCustomer(id);runLoad()}}/>} {tab==="Sales / POS"&&<POS products={active} sales={data.sales} onDone={async d=>{const r=await createSale(d);setModal(null);runLoad();return r}} canFinance={context.owner || context.canFinance} onRefund={async id=>{if(confirm("Refund this sale and restore its inventory?")){await refundSale(id);runLoad()}}} money={money}/>} {tab==="Expenses"&&<Expenses items={data.expenses} onAdd={()=>{setEditing(null);setModal("expense")}} onEdit={x=>{setEditing(x);setModal("expense")}} onDelete={async id=>{await deleteExpense(id);runLoad()}} money={money}/>} {tab==="Suppliers"&&<Suppliers items={data.suppliers} purchases={data.purchases} onAdd={()=>{setEditing(null);setModal("supplier")}} onEdit={x=>{setEditing(x);setModal("supplier")}} onPurchase={()=>setModal("purchase")} onDelete={async id=>{await deleteSupplier(id);runLoad()}} money={money} canFinance={context.owner || context.canFinance} onReversePurchase={async id=>{if(confirm("Reverse this purchase? This will restore the recorded stock quantity and mark the purchase reversed.")){try{await reversePurchase(id);runLoad()}catch(e){alert(e?.response?.data?.message||"This purchase cannot be reversed because subsequent inventory activity exists or the purchase is not eligible for reversal.")}}}}/>} {tab==="Reports"&&<Reports report={report} setReport={setReport} money={money}/>} {tab==="Settings"&&<Settings type={type} business={business} owner={context.owner} onBack={goBack}/>}</div></main>{modal==="product"&&<ProductModal item={editing} currency={businessCurrency} canFinance={context.owner || context.canFinance} onClose={()=>setModal(null)} onSave={async d=>{editing?await updateProduct(editing.id,d):await createProduct(d);setModal(null);runLoad()}}/>}{modal==="expense"&&<ExpenseModal item={editing} onClose={()=>setModal(null)} onSave={async d=>{editing?await updateExpense(editing.id,d):await addExpense(d);setModal(null);runLoad()}}/>}{modal==="purchase"&&<PurchaseModal products={active} suppliers={data.suppliers} onClose={()=>setModal(null)} onSave={async d=>{await createPurchase(d);setModal(null);runLoad()}}/>}{modal==="customer"&&<CustomerModal item={editing} onClose={()=>setModal(null)} onSave={async d=>{editing?await updateCustomer(editing.id,d):await createCustomer(d);setModal(null);runLoad()}}/>}{modal==="supplier"&&<SupplierModal item={editing} onClose={()=>setModal(null)} onSave={async d=>{editing?await updateSupplier(editing.id,d):await addSupplier(d);setModal(null);runLoad()}}/>}</div>;
}
